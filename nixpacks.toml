[phases.setup]
nixPkgs = ["python39", "python39Packages.pip", "zlib", "gcc"]

[phases.install]
commands = [
    "python -m ensurepip --upgrade",
    "pip install -r requirements.txt"
]

[phases.start]
command = "gunicorn app:app --bind 0.0.0.0:$PORT"
